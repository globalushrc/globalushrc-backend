{
  "name": "global-us-hrc-root",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "backend": "npm run start --prefix backend",
    "install-all": "npm install --prefix frontend && npm install --prefix backend"
  }
}
